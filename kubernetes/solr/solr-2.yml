apiVersion: v1
kind: ReplicationController
metadata:
  name: solr-controller-2
  namespace: default
  labels:
    app: solr-controller-2
spec:
  replicas: 1
  selector:
    app: solr-app-2
  template:
    metadata:
      labels:
        app: solr-app-2
    spec:
      hostNetwork: true
      containers:
      - name: solr-container-2
        image: solr:7.5
        ports:
        - name: solr-2
          containerPort: 8983
          protocol: TCP
        env:
        - name: SOLR_HOST
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: ZK_HOST
          value: 192.168.152.201:32181
        - name: SOLR_DATA_HOME
          value: /data
        - name: SOLR_HEAP
          value: 16G
        volumeMounts:
        - mountPath: /data
          name: data
      nodeSelector:
          kubernetes.io/hostname: node2
      volumes:
      - name: data
        hostPath:
            path: /hdd2/data/solr
            type: Directory
