apiVersion: v1
kind: ReplicationController
metadata:
  name: solr-controller-4
  namespace: default
  labels:
    app: solr-controller-4
spec:
  replicas: 1
  selector:
    app: solr-app-4
  template:
    metadata:
      labels:
        app: solr-app-4
    spec:
      hostNetwork: true
      containers:
      - name: solr-container-4
        image: solr:7.5
        ports:
        - name: solr-4
          containerPort: 8984
          protocol: TCP
        env:
        - name: SOLR_HOST
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: ZK_HOST
          value: 192.168.152.201:32181
        - name: SOLR_DATA_HOME
          value: /data
        volumeMounts:
        - mountPath: /data
          name: data
      nodeSelector:
          kubernetes.io/hostname: node4
      volumes:
      - name: data
        hostPath:
            path: /hdd2/data/solr
            type: Directory
